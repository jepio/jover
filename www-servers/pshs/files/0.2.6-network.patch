--- a/src/network.c	2015-07-10 02:10:20.792035512 +0200
+++ b/src/network.c	2015-07-10 02:04:33.096027094 +0200
@@ -85,7 +85,7 @@ const char* init_external_ip(unsigned in
 #endif
 					strport, strport, lan_addr,
 					"Pretty small HTTP server",
-					"tcp",
+					"TCP",
 #ifdef LIBMINIUPNPC_SO_8
 					NULL,
 #endif
@@ -139,7 +139,7 @@ void destroy_external_ip(unsigned int po
 #else
 				upnp_data.servicetype,
 #endif
-				strport, "tcp", NULL);
+				strport, "TCP", NULL);
 		if (ret != UPNPCOMMAND_SUCCESS)
 			fprintf(stderr, "UPNP_DeletePortMapping() failed: %s\n", strupnperror(ret));
 		FreeUPNPUrls(&(upnp_urls));
